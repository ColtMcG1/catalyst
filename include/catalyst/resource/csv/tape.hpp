#pragma once

namespace catalyst::resource::csv {

    class tape {
    public:
        tape() = default;
        ~tape() = default;
    };

    class cursor {
    public:
        cursor() = default;
        ~cursor() = default;
    };

}